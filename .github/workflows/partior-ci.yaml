name: Quorum Signer Plugin CI
on:
  pull_request:
    branches: [ main, release/**, hotfix/** ]
  push:
    branches: [ main ]
  workflow_dispatch:  
jobs:
  ci-pipeline:
    uses: partior-libs/controller-3p-pipelines/.github/workflows/pipeline-template-quorum-signer-plugin-ci.yaml@feature/DSO-2703-quorum-signer
    with:
      CONTROLLER-REPO: partior-libs/controller-3p-pipelines
      PROJECT-REPO: ${{ github.repository }}
      CONTROLLER-REF: feature/DSO-2703-quorum-signer
    secrets: inherit